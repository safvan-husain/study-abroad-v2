'use client';
import { useState } from 'react'; import { useChatConversation } from '../../hooks/useChatConversation'; import { ChatMessage } from './ChatMessage';
export function ChatPanel({ conversationId='00000000-0000-4000-8000-000000000001' }: { conversationId?: string }) { const chat=useChatConversation(conversationId); const [value,setValue]=useState(''); const submit=async(e:React.FormEvent)=>{e.preventDefault();if(value.trim()){await chat.send(value.trim());setValue('')}}; return <section className="chat-panel" aria-label="Chat conversation"><header><span>First chatbot slice</span><strong>{chat.status?.status ?? 'ready'}</strong></header><div className="chat-messages">{chat.loading?<p>Loading conversation...</p>:chat.messages.map(m=><ChatMessage key={m.messageId} message={m}/>)}{chat.status?.status==='pending'&&<p>Assistant is thinking...</p>}{chat.error&&<div role="alert">{chat.error} <button onClick={()=>void chat.reload()}>Retry</button></div>}</div><form onSubmit={submit}><input aria-label="Message" value={value} onChange={e=>setValue(e.target.value)} placeholder="Ask a question"/><button type="submit">Send</button></form></section>; }
